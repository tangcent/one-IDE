# Project Rules

- **Cross-Plugin Updates**: When updating the code of any plugin (e.g., JetBrains plugin), always consider if the same update needs to be applied to other plugins (e.g., VS Code extension) to maintain feature parity and consistency.